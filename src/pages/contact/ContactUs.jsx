import ContInfo from "./ContInfo";
import Hero from "./Hero";

const ContactUs = () => {
    return (
        <main>
            <Hero/>
            <ContInfo/>
        </main>
    );
};

export default ContactUs;